python-docx
pypdf

# Optional local OCR foundation; Tesseract itself is a separate local system dependency.
pytesseract
Pillow
PyMuPDF

# Optional local NER/NLP foundation; Polish spaCy models are installed separately.
spacy

# GUI: modern flat theming and native drag-and-drop for the desktop app.
customtkinter
tkinterdnd2

# Version comparison for the startup library-update check (dependency_updates.py).
packaging
